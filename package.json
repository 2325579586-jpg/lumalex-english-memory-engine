{
  "name": "world-app-api",
  "private": true,
  "scripts": {
    "check": "npm run check:api && npm test && npm --prefix frontend run build",
    "check:api": "node tests/check-api-syntax.js",
    "test": "npm run test:api && npm run test:backend",
    "test:api": "node --test tests/api-security.test.js",
    "test:backend": "python -m unittest discover -s tests"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4"
  }
}
